First try to understand the codebase, especially the data schema, and how it's used in the critical user journey.
My goal here is to make this demo app immediately usable and demoable to users. 
I want you to use up to 5 sub-agents (to save context window) to complete the following:
  1. Create a comprehensive range of demo data so that users can jump on and try to use the application. Make sure you cover `Demo Project`, `Demo Template` and `Demo Library` in the current workspace.
  2. Create a help guide to help users learn how to use the application. Write it in .md files, and make sure it's rendered as structured set of pages, which can be access via a new nav tab 'Guide' in top nav.